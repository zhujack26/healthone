class UserInfo {
  constructor(userName, userEmail, joinDate, userStatus) {
    this.userName = userName;
    this.userEmail = userEmail;
    this.joinDate = joinDate;
    this.userStatus = userStatus;
  }
}

export default UserInfo;
